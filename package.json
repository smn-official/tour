{
  "name": "@smn-official/tour",
  "version": "0.0.0",
  "repository": "https://github.com/smn-official/tour.git",
  "author": "SMN Informática",
  "license": "MIT",
  "scripts": {
    "demo": "tsnd --transpile-only --ignore-watch node_modules --respawn demo-server.ts",
    "build:typescript": "yarn tsc --build tsconfig.prod.json",
    "build:webpack": "yarn webpack",
    "build:clean": "yarn rimraf dist",
    "build": "yarn build:clean & yarn build:typescript & yarn build:webpack"
  },
  "devDependencies": {
    "@types/node": "^14.10.1",
    "ts-loader": "^8.0.3",
    "ts-node-dev": "^1.0.0-pre.62",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "typescript": "^4.0.2"
  }
}
